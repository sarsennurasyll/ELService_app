abstract final class AppDuration {
  static const dialog = Duration(milliseconds: 200);
  static const sheetClose = Duration(milliseconds: 300);
  static const sheetOpen = Duration(milliseconds: 500);
  static const reveal = Duration(milliseconds: 600);
  static const extended = Duration(milliseconds: 1000);
  static const liveStatus = Duration(milliseconds: 1800);
}
